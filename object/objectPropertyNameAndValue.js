const person = {
    firstName : "Eko",
    lastName : "Kurniawan"
};

// Object.value() => Untuk mengambil semua property value
console.log(Object.values(person));
// Object.getOwnPropertyNames() => Untuk mengambil semua property name
console.log(Object.getOwnPropertyNames(person));